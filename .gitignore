# Build products
.build/
.swiftpm/
DerivedData/
*.o
*.dSYM/

# The app bundle is a build artefact; releases are published, not committed.
Jendela.app/
WidgetMac.app/   # old name, ignore leftovers

# The Xcode project is generated by Support/generate_project.rb.
# Committing it would mean two sources of truth for the build.
*.xcodeproj/
*.xcworkspace/
xcuserdata/
*.xcuserstate

# macOS
.DS_Store
.AppleDouble
Icon?

# Local secrets and environment — never commit these.
.env
.env.*
!.env.example
*.p12
*.mobileprovision
*.provisionprofile

# Editors
.idea/
.vscode/
*.swp
jendela-licence-private.key
Support/release.env
dist/
server/.env
server/*.db
server/*.db-wal
server/*.db-shm
node_modules/
